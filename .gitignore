__pycache__/
*.pyc
.venv/
venv/
.env
*.egg-info/
dist/
build/
.DS_Store
.mcp.json
vision_cache/
services/ml-api/data/*.json
!services/ml-api/data/rag_eval_qa.json
!services/ml-api/data/rag_eval_mm_qa.json
services/ml-api/routers/document/_corrections_store.json

# Local artifacts
.playwright-mcp/
conversation.md
*.png
test_clusters.csv
# Scratch files from testing the document tools by hand. Untracked once
# already: they reached main inside a commit whose work belonged to the
# other repository, swept up by a `git add -A` run from the wrong
# directory. Ignoring them means a repeat of that cannot pick them up.
/test_invoice.pdf
/test_llm_invoice_items.py
/test_pipeline.csv

# Generated by a run, never edited by hand — rebuilt on demand, and the
# ChromaDB is a live 49MB file that would add a new blob to the repo on
# every commit that touched it.
services/ml-api/catboost_info/
services/ml-api/data/chroma_db/
services/ml-api/models/ci-test-*
services/ml-api/schemas/ci-test-*.json
