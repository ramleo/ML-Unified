scikit-learn==1.8.0
pandas==2.2.2
numpy==1.26.4
joblib==1.5.3
xgboost==2.1.1
lightgbm==4.6.0
catboost==1.2.10
shap==0.47.2
optuna>=3.6.0
imbalanced-learn>=0.12.0
sentence-transformers>=3.0.0,<6.0.0  # 6.0.0 hard-requires transformers>=5.0, conflicts with the pin below
transformers>=4.41.0,<4.49.0
tokenizers>=0.20,<0.21
einops>=0.7.0
chromadb>=0.5.0
rank-bm25>=0.2.2
onnxruntime>=1.18.0
timm>=1.0.0  # ResNet backbone for microsoft/table-transformer-detection (mm_tables.py)
# facenet-pytorch is installed separately in the Dockerfile with --no-deps: it
# pins torch<2.3.0, which conflicts with sam2's torch>=2.5.1 floor below. Its
# own actual runtime deps (numpy, Pillow, requests, tqdm) are already satisfied
# by pins elsewhere in this file / requirements.txt; only torch/torchvision are
# left unpinned by facenet-pytorch so they resolve to satisfy sam2 instead.
langgraph>=0.2.0
langchain-core>=0.3.0
spacy>=3.7.0,<3.8.0
dnspython>=2.6.0  # SPF/DMARC/DKIM DNS TXT lookups (email_auth_check.py)
sam2>=1.1.0  # video object tracking (mm_rotoscope.py); Apache 2.0, ungated
# groundingdino-py + supervision are installed separately in the Dockerfile with
# --no-deps: both declare an unpinned, non-headless opencv-python dependency that
# would collide with opencv-python-headless above (and typically fails to import
# in a slim Docker image at all, missing libGL.so.1). Their actual other runtime
# deps (verified locally — groundingdino.util.inference only needs cv2.imread/
# cvtColor-level calls, which opencv-python-headless already provides) are listed
# here so they resolve normally alongside the rest of this file's pins.
addict>=2.4.0       # groundingdino-py dependency (mm_rotoscope.py)
pycocotools>=2.0.0  # groundingdino-py dependency (mm_rotoscope.py)
yapf>=0.40.0        # groundingdino-py dependency (mm_rotoscope.py)
matplotlib>=3.9.0   # supervision dependency (mm_rotoscope.py)
